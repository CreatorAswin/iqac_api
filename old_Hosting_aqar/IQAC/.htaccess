# Allow direct file access for IQAC documents
# This file ensures uploaded documents are accessible via direct URLs

# Enable following symlinks
Options +FollowSymLinks

# Prevent directory listing
Options -Indexes

# Allow access to all files in this directory
<FilesMatch ".*">
    Order allow,deny
    Allow from all
</FilesMatch>

# Set proper MIME types for common document formats
AddType application/pdf .pdf
AddType application/msword .doc
AddType application/vnd.openxmlformats-officedocument.wordprocessingml.document .docx
AddType application/vnd.ms-excel .xls
AddType application/vnd.openxmlformats-officedocument.spreadsheetml.sheet .xlsx
AddType image/jpeg .jpg .jpeg
AddType image/png .png
AddType image/gif .gif

# Force download for certain file types (optional - comment out if you want inline viewing)
# <FilesMatch "\.(pdf|doc|docx|xls|xlsx)$">
#     Header set Content-Disposition "attachment"
# </FilesMatch>
